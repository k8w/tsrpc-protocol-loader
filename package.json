{
  "name": "tsrpc-protocol-loader",
  "version": "0.1.0",
  "description": "Extended native JS",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "build": "rm -rf dist && tsc -d && cp package.json LICENSE README.md dist/ && find dist/test -name \"*.d.ts\" | xargs rm -rf"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/k8w/k8w-xxx.git"
  },
  "keywords": [
    "k8w",
    "tsrpc"
  ],
  "author": "k8w",
  "license": "Apache-2.0"
}
